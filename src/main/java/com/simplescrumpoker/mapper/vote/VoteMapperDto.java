package com.simplescrumpoker.mapper.vote;

import com.simplescrumpoker.mapper.MappableDto;
import com.simplescrumpoker.mapper.MapperDto;
import com.simplescrumpoker.model.Vote;

public interface VoteMapperDto<D extends MappableDto> extends MapperDto<Vote, D> {
}
