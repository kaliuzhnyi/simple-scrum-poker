package com.simplescrumpoker.mapper.retro.message;

import com.simplescrumpoker.mapper.MappableDto;
import com.simplescrumpoker.mapper.MapperDto;
import com.simplescrumpoker.model.retro.Retro;
import com.simplescrumpoker.model.retro.RetroMessage;

public interface RetroMessageMapperDto<D extends MappableDto> extends MapperDto<RetroMessage, D> {
}
