package com.simplescrumpoker.mapper.guest;

import com.simplescrumpoker.mapper.MappableDto;
import com.simplescrumpoker.mapper.MapperDto;
import com.simplescrumpoker.model.guest.Guest;

public interface GuestMapperDto<D extends MappableDto> extends MapperDto<Guest, D> {
}
